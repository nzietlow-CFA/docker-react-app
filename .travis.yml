language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t nzietlow/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true nzietlow/docker-react npm run test -- --coverage
